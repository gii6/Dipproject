<%= form_for(@additional_day_info) do |f| %>
  <% if @additional_day_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@additional_day_info.errors.count, "error") %> prohibited this additional_day_info from being saved:</h2>

      <ul>
      <% @additional_day_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br>
    <%= f.date_select :day %>
  </div>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.select :style, selectify(AdditionalDayInfo::DAYSTYLE),{},class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :year_id %><br>
    <%= f.text_field :year_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
